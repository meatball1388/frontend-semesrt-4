# PWA приложение «Заметки» — Практические занятия №13-18

## Описание
Объединённое PWA-приложение для управления заметками и задачами с офлайн-доступом, WebSocket-синхронизацией и Push-уведомлениями. Реализует требования всех практических занятий №13-18.

---

## Практика №13 — Service Worker
- **Service Worker** — кэширование статических ресурсов (Cache First)
- **Офлайн-режим** — приложение работает без интернета
- **localStorage** — хранение заметок на клиенте

## Практика №14 — Web App Manifest
- **manifest.json** — полный манифест с 10 иконками (72x72 — 512x512 + favicon)
- **PWA-установка** — приложение можно установить на устройство
- **Мета-теги** — apple-mobile-web-app, theme-color, mobile-web-app-capable
- **generate_icons.py** — скрипт для генерации иконок (Python + Pillow)

## Практика №15 — HTTPS + App Shell
- **App Shell архитектура** — мгновенная загрузка каркаса приложения
- **Два кэша**: `app-shell-v3` (статика) и `dynamic-content-v2` (динамический контент)
- **Cache First** для статики, **Network First** для динамических страниц (`/content/*`)

## Практика №16 — WebSocket + Push
- **WebSocket (Socket.IO)** — real-time уведомления между клиентами
- **Push-уведомления** — системные уведомления даже при закрытом приложении (Web-Push, VAPID)
- **Toast-уведомления** — всплывающие уведомления о новых заметках от других клиентов
- **VAPID ключи** — в файле `vapid-keys.json`
- **Эндпоинты подписки/отписки**: `/subscribe`, `/unsubscribe`
- **Кнопки управления уведомлениями** — включение/отключение push из интерфейса
- **Индикатор онлайн/офлайн** — статус подключения в шапке приложения

## Практика №17 — Детализация Push (Напоминания)
- **Форма напоминаний** — текст + дата/время (`datetime-local`)
- **Планирование на сервере** — `setTimeout` с хранением таймеров в `Map`
- **Push с кнопкой "Отложить"** — Service Worker обрабатывает действие `snooze`
- **Эндпоинт `/snooze`** — перенос напоминания на 5 минут
- **Валидация времени** — напоминание только на будущее время

## Практика №18 — Подготовка к контрольной работе №3
- Тестирование всех компонентов
- Данный README с описанием проделанной работы

---

## Структура проекта

```
frontend-merged/
├── content/
│   ├── home.html          # Главная страница (формы заметок + напоминаний + список)
│   └── about.html         # Страница "О приложении"
├── icons/                  # Иконки приложения (11 файлов: favicon + 8 размеров + maskable)
├── index.html              # App Shell каркас с meta-тегами и статусом подключения
├── app.js                  # Клиентская логика (навигация, заметки, WebSocket, Push, online/offline)
├── sw.js                   # Service Worker (2 кэша + push-обработка + snooze)
├── server.js               # Сервер (Express + Socket.IO + Web-Push + напоминания + snooze)
├── style.css               # Дополнительные стили
├── manifest.json           # Web App Manifest (10 иконок)
├── vapid-keys.json         # VAPID ключи
├── generate_icons.py       # Скрипт генерации иконок
├── package.json
├── .gitignore
└── README.md               # Этот файл
```

---

## Установка и запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск сервера
```bash
node server.js
```
Сервер запустится на `http://localhost:3001`

### 3. Генерация иконок (опционально)
Для перегенерации иконок нужен Python с библиотекой Pillow:
```bash
pip install Pillow
python generate_icons.py
```

---

## Проверка работы

### WebSocket (Практика №16)
1. Откройте две вкладки браузера: `http://localhost:3001`
2. Добавьте заметку в первой вкладке
3. Во второй вкладке появится toast-уведомление о новой заметке

### Push-уведомления (Практика №16)
1. Нажмите "Включить уведомления" и разрешите их в браузере
2. Добавьте заметку в одной вкладке
3. В другой вкладке (или при закрытом приложении) придёт системное push-уведомление

### Напоминания (Практика №17)
1. Нажмите "Добавить с напоминанием"
2. Введите текст и выберите дату/время в будущем
3. В назначенное время придёт push-уведомление с кнопкой "Отложить на 5 минут"
4. Нажмите "Отложить" — напоминание перенесётся на 5 минут

### Service Worker (Практика №15)
1. Откройте DevTools → Application → Service Workers
2. Проверьте, что SW активен
3. В Cache Storage должно быть два кэша: `app-shell-v3` и `dynamic-content-v2`

### Офлайн режим (Практика №13)
1. Отключите сеть в DevTools (Offline)
2. Обновите страницу — приложение загрузится из кэша
3. Заметки будут доступны, можно добавлять новые

### PWA установка (Практика №14)
1. В Chrome появится иконка "Установить приложение" в адресной строке
2. Также можно проверить через DevTools → Application → Manifest

---

## Технологии

| Технология | Назначение |
|-----------|-----------|
| **Express.js** | Сервер и раздача статики |
| **Socket.IO** | WebSocket для real-time уведомлений |
| **Web-Push** | Push-уведомления с VAPID |
| **Service Worker** | Кэширование, офлайн, push-обработка |
| **localStorage** | Хранение заметок на клиенте |
| **Chota CSS** | CSS-фреймворк (CDN) |
| **Pillow (Python)** | Генерация иконок |

---

## Архитектура

### App Shell
`index.html` содержит минимальный каркас (шапка, навигация, контейнер). Контент подгружается динамически через `fetch` в `app.js`.

### Стратегии кэширования
- **Cache First** — статика (HTML, CSS, JS, иконки, манифест)
- **Network First** — динамические страницы (`/content/*`)

### Модель данных заметки
```javascript
{
    id: 1712345678901,       // timestamp
    text: "Текст заметки",
    reminder: 1712349278901   // timestamp напоминания (null если нет)
}
```

### Поток напоминаний
1. Клиент → `socket.emit('newReminder', {id, text, reminderTime})`
2. Сервер → `setTimeout` с задержкой `reminderTime - Date.now()`
3. Сервер → `webpush.sendNotification()` + `io.emit('taskAdded')`
4. Клиент получает push с кнопкой "Отложить на 5 минут"
5. Клик → `fetch('/snooze?reminderId=...')` → сервер перепланирует на 5 мин

---

## Авторы
Практические занятия №13-18, кафедра Индустриального программирования
